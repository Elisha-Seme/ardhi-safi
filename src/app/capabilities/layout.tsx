import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Our Capabilities",
    description: "Explore Ardhi Safi's comprehensive real estate services including sales, letting, property management, and investment advisory.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
