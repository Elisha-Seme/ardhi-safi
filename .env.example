# Database connection string
# For SQLite (development):
DATABASE_URL="file:./dev.db"
# For PostgreSQL (production):
# DATABASE_URL="postgresql://user:password@host:5432/ardhi_safi"

# NextAuth configuration
# Generate a strong secret with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-a-cryptographically-random-secret"
NEXTAUTH_URL="http://localhost:3000"

# Public site URL (used in sitemap, OpenGraph, etc.)
NEXT_PUBLIC_SITE_URL="https://ardhisafi.co.ke"
